# Subset del catálogo (backend/forensia/toolkit/catalog.py) que este agente
# puede invocar. Todos los ids listados deben (1) existir en el catálogo y
# (2) declarar el os_profile "unix"; en caso contrario, la carga falla.
allowed:
  - tsk_mmls
  - tsk_fls
  - tsk_mactime
  - ewf_info
  - bulk_extractor
  - yara
  - volatility3
  - jq
