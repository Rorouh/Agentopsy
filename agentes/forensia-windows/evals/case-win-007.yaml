# Caso de evaluación sintético para el harness comparativo local-vs-cloud.
# Formato propuesto en docs/agentes/diseno-fase2.md §9.4.
# NUNCA contiene evidencia real con datos personales: la fixture es sintética.
id: case-win-007
description: "Imagen Windows 10 con borrado del log de seguridad: evento 1102 + hueco en la secuencia de RecordId"
os_profile: windows
evidence_fixture: fixtures/win10-log-clear.raw   # generada por el equipo, no incluida en git

expected_findings:
  - title_glob: "*1102*|*borrado*log*|*event log cleared*|*hueco*secuencia*"
    severity: high
    provenance_tool: evtxecmd         # Security.evtx 1102 (the audit log was cleared) + salto de RecordId

expected_mitre:
  - technique_id: T1070.001          # Indicator Removal: Clear Windows Event Logs

budget:
  max_iterations: 12
  max_tokens: 200000

# El 1102 (evtxecmd) es la evidencia directa; el hueco en la secuencia de RecordId lo
# corrobora. Barrido de refuerzo con hayabusa/chainsaw (regla Sigma de log clear).
