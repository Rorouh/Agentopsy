# Instala SOLO lo del backend que vayas a usar (los providers se importan de forma perezosa).

# --- Backend con tu SUSCRIPCIÓN Claude (Pro/Max), sin API key de pago ---
# Trae el CLI de Claude Code incluido. Después: ejecuta `claude` y haz /login.
claude-agent-sdk>=0.1.0

# --- Backend local (privacidad máxima) ---
ollama>=0.3.0

# --- Backends cloud (opcionales) ---
anthropic>=0.39.0
openai>=1.40.0

# --- Informe a PDF (opcional; si no, el informe sale en Markdown) ---
markdown>=3.5
weasyprint>=60
