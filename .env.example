# =============================================================================
# Agentopsy — variables opcionales del compose (copiar a .env solo si hace falta)
#
# Este proyecto NO usa API keys (SECURITY INVARIANT 7): aquí nunca habrá
# ANTHROPIC_API_KEY / OPENAI_API_KEY ni equivalentes. Los ejecutores CLI se
# autentican con la sesión del volumen `agentopsy-cli-auth`, seeded una vez
# desde las credenciales del host (o con login directo en el contenedor).
# =============================================================================

# HOME: docker-compose.yml la usa para montar en SOLO LECTURA el staging de
# credenciales que el entrypoint seedea al volumen en el primer arranque:
#   ${HOME}/.claude  ${HOME}/.claude.json  ${HOME}/.codex  ${HOME}/.gemini
#
# - Linux / macOS: la shell ya define HOME — no necesitas este fichero.
# - Windows (PowerShell / cmd fuera de WSL): HOME puede no estar definida.
#   Copia este fichero a .env y apunta a tu perfil de usuario, p. ej.:
# HOME=C:\Users\tu-usuario

# AGENTOPSY_EXECUTOR_TIMEOUT: segundos que una petición a un ejecutor EN NUBE
# (Claude Code, Codex CLI, Gemini CLI) puede tardar antes de abortarse (y
# auditarse) como timeout. Sin definir, el backend usa su default de diseño
# (300 s). Súbelo si tu modelo en nube responde lento, p. ej.:
# AGENTOPSY_EXECUTOR_TIMEOUT=600
#
# Ollama NO lleva límite: es 100 % local, no lo factura nadie por segundo, y un
# modelo grande tarda minutos por turno y bastante más en redactar un informe
# pericial entero. Esta variable no le afecta.

# AGENTOPSY_EVIDENCE_DIR / AGENTOPSY_CASES_DIR: carpetas del HOST que el compose
# monta como /evidence (SOLO LECTURA) y /cases en api y maletines. El diseño de
# la app es "una carpeta de evidencia elegida por el usuario": apunta aquí a la
# carpeta del caso en vez de copiar la evidencia dentro del repo. Sin definir
# se usan ./evidence y ./projects (defaults de diseño; ver docker/README.md,
# apartado «Carpeta de evidencia configurable»). Nunca commitees rutas de tu host.
# AGENTOPSY_EVIDENCE_DIR=/ruta/al/caso/evidencia
# AGENTOPSY_CASES_DIR=/ruta/al/caso/salidas
